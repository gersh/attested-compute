# Versioned algorithm specifications

Files in this directory are protocol inputs, not mutable tutorials. Their
exact bytes may be hashed into reports and run bundles.

`REAL_ZETA_POC.md` is the immutable definition of
`sparkinterval.real_zeta_integer_dirichlet_integral_tail.v1`. Its SHA-256 is
`9a3bd6af5548d2c8c882f30787e4fe1170babca78143a24d40523fbf72ec6cb9`.
Changing the algorithm requires a new identifier and a new specification;
editing the v1 file in place would break verification of existing v1 records.

For current commands and explanatory material, use the mutable
[real-zeta tutorial](../docs/algorithms/REAL_ZETA_POC.md).

`TERNARY_GOLDBACH_EXTERNAL_ATOMS.json` is the machine-readable catalog of the
thirteen source-shaped external atoms currently used by the `claude_math`
ternary Goldbach theorem.  It fixes each Lean declaration name, exact claim,
present evidence, missing completion requirement, feasibility class, and
work-unit count.  The catalog is an audit and planning input; it does not
declare that an external artifact has discharged a Lean atom.

`TERNARY_GOLDBACH_CAMPAIGN_PROFILES.json` is the fail-closed execution-profile
registry for exactly those same thirteen IDs. It binds itself to the raw
SHA-256 of the external-atom catalog and records each full source domain,
required input, implemented or missing engine, resume capability, and exact
assurance flags. A profile describes capability only: it is not a run receipt,
and `sample_only` engines are forbidden from advertising full-source support.
See the [campaign control-plane guide](../docs/algorithms/TERNARY_GOLDBACH_CAMPAIGNS.md).

`TERNARY_GOLDBACH_COMPACT_RECEIPT_CLOSURE.json` is the cross-layer closure
matrix for those thirteen atoms plus the distinct lowered finite endpoint
below `10^27`. It records the eleven physical campaigns, exact registered
result and logical-identity pins, existing conditional Lean soundness
theorems, missing machine/compiler and native-evidence refinements, and
whether a direct or transitive campaign receipt is required. Null physical
pins and false authority flags are intentional before a reviewed cloud run.
See the [compact receipt closure guide](../docs/algorithms/TERNARY_GOLDBACH_COMPACT_RECEIPT_CLOSURE.md).

`TERNARY_GOLDBACH_EXTERNAL_PROGRAM_READINESS.json` is the source audit of the
ten physical campaigns serving the thirteen named atoms. It distinguishes a
full-range external route, an operational retained artifact, a complete Lean
source artifact, Azure materialization, installed deployment pins, and
reviewed receipt authority. Its current single source-artifact-complete entry
is CDEM Abel; that status does not assert a machine refinement or run.

`TERNARY_GOLDBACH_EXTERNAL_COMPLETION_AUDIT.json` applies the stricter
production-completion conjunction to those same ten campaigns: optimized
source-scale executable, complete Lean source artifact, independent checker,
Lean parser/total checker/soundness, an explicit machine-trust route,
confidential Azure packaging, measured target-SKU high endpoints below one
week and USD 10,000, an admitted production receipt, and live `claude_math`
integration. The companion
`tools/validate_tg_external_completion.py --require-claude-math` command
cross-checks that matrix against both source trees when the sibling checkout
is available. The current count is deliberately zero complete campaigns.

`TERNARY_GOLDBACH_REGISTERED_CAMPAIGN_MATRIX.json` fixes the eleven terminal
registration chains: the ten named physical campaigns plus the distinct
lowered-Goldbach alternate. The bounded
`tools/audit_tg_registered_campaigns.py --check` command cross-checks Lean
identities, generated hashes, terminal factories, CPU/H100 backends,
registered-result paths, materializers, and the enabled/staged/null semantic
state. All ten named physical campaigns now have reviewed terminal result
contracts; one semantic row is enabled and ten rows (including the distinct
lowered alternate) remain staged-disabled. The audit explicitly reports zero
analytic realizations and zero production runs.

`TERNARY_GOLDBACH_NATIVE_MEMBER_CROSSWALK.json` is the production-data-free
member audit for all 1,371 native-generated roots in the last-fresh
`ternary_goldbach` manifest. It keeps the pinned 1,081/1/289 historical
projection separate from the later 1,085/5/281 dirty-tree diagnostic and
records an exact target location for all 1,371 members. A target mapping is only
staged source evidence: it does not assert proposition identity, live
integration, or retirement. See the
[native member crosswalk guide](../docs/algorithms/TERNARY_GOLDBACH_NATIVE_MEMBER_CROSSWALK.md).

`TERNARY_GOLDBACH_FULL_TRUST_BOUNDARY.json` joins the last-fresh 1,387-root
capstone snapshot to the external, native-family, member, source-program, and
receipt audits. Its receipt boundary fixes exactly eleven proof-authorizing
slots: ten external physical campaigns and one all-native aggregate. The
optional Ramaré fallback is not a twelfth theorem root. The checked-in state
records zero imported receipts and zero accepted receipts, so this inventory
does not claim discharge or retirement.

The shared bounded CUDA producer for the CDEM squarefree and Hurst/Mertens
entries is `tg_mobius_segment_v1`.  Its runner emits a deterministic
`tg_mobius_transition_lines_v1` hash chain and independently CPU-checks every
GPU row.  This implementation detail supplements the two source-shaped
catalog entries; it is not a new atom and does not make an incomplete prefix
chain into a full-range certificate. The Python receipt-chain checker only
checks self-reported structure and composition: it neither replays the rows
nor authenticates executable or GPU execution.

For checker commands, the evidence-class vocabulary, measured local timings,
and explicitly labeled H100 planning ranges, read the
[ternary Goldbach external-atoms guide](../docs/algorithms/TERNARY_GOLDBACH_EXTERNAL_ATOMS.md).

`TG_EQ1315_COUPLED_CAP_DISABLED.json` fixes the disabled prototype contract
for the exact, endpoint-aware Chapter-14 equation-(13.15) boundary. It records
the correlated `(u,v,t)` cells, pointwise `q` and delta guards, parity lanes,
finite directed-Gaussian checker, and the deliberately empty tail-witness
registry. It is not a successful certificate or receipt; see the
[equation-(13.15) campaign guide](../docs/algorithms/EQ1315_COUPLED_CAP_CAMPAIGN.md).

`SQRT218_AZURE_CPU_READINESS.json` is the fail-closed readiness record for the
finite equation-(2.18) certificate through `2,000,000`. It distinguishes the
implemented CPU measured job, independent replay, corpus protocol, state
machine, and KAT from the absent real corpus pin, Azure policies/run/receipt,
architecture refinement proof, and Lean admission. It also records the
separate fixed-width V2 C/Lean checker and compact receipt path: ordinary
local verification needs no production archive or arithmetic replay, while
the fixed-V2 cloud job, production certificate integration, native refinement,
reviewed pins, and signed receipt remain absent.
